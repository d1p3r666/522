# Лабораторная работа по компьютерным сетям

**Студент:** Майоров Даниил Викторович  
**Группа:** 28ИПо8481  

---

## 1. Как вообще сети появились?

Сети развивались от маленьких локальных экспериментов до всемирного интернета.

**Что было когда:**

| Год | Событие |
|-----|---------|
| Начало 70-х | Первые локальные сети, ещё нестандартные |
| 1971 | Рей Томлинсон (BNN) сделал программу для email |
| 1973 | Роберт Меткалф из Xerox придумал Ethernet |
| 1974 | Серф и Кан разработали протокол TCP |
| 1978–79 | Создали модель OSI |
| Начало 80-х | TCP/IP стал основным, ARPANET перешла на него |
| 1985 | Стандарты Ethernet, Token Ring, FDDI |
| 1988 | Червь Морриса заразил ~6000 компьютеров, создали CERT |
| 1991–92 | Тим Бернерс-Ли придумал WWW |
| 1994 | Появился протокол PPP |
| 1995 | NSFNet заменили коммерческой сетью, появились провайдеры |

---

## 2. Как устроено сетевое общение

**Протокол** — это правила, по которым устройства общаются.  
**Стек протоколов** — несколько уровней таких правил.

### 2.1. Модель OSI (7 уровней)

Снизу вверх:

1. **Физический** — передача битов по кабелю (витая пара и т.п.)
2. **Канальный** — кадры, MAC-адреса, коммутаторы
3. **Сетевой** — пакеты, IP-адреса, маршрутизация
4. **Транспортный** — сегменты/дейтаграммы, TCP и UDP
5. **Сеансовый** — управление сессией (пример — SSH)
6. **Представления** — шифрование, сжатие данных
7. **Прикладной** — сообщения, протоколы HTTP, FTP и т.д.

### 2.2. Модель TCP/IP (DoD)

В ней 4 уровня:

- **Прикладной** (объединяет 5,6,7 уровни OSI)
- **Транспортный** (как в OSI)
- **Межсетевой** (соответствует сетевому уровню OSI)
- **Доступа к сети** (объединяет 1 и 2 уровни OSI)

---

## 3. Прикладной уровень

Сетевые приложения бывают:

- **Клиент-сервер** — один запрашивает, другой отвечает (браузер и веб-сервер)
- **P2P** — все участники равны

**Сокет** — это IP-адрес + порт (например, для HTTP порт 80, для SMTP — 25)

---

## 4. DNS — система доменных имён

Нужна, чтобы превращать понятные имена (`google.com`) в IP-адреса.

**Типы доменных имён:**
- Короткие
- Относительные
- Полные (FQDN)

**Типы доменов верхнего уровня:**
- Родовые: .com, .edu, .org
- Национальные: .ru, .by, .us
- Спонсируемые

**Типы записей DNS:**
- A — IP-адрес хоста
- NS — сервер имён
- CNAME — псевдоним
- MX — почтовый сервер

**Как Windows ищет адрес:**
1. Проверяет, не локальный ли это узел
2. Смотрит в локальный кэш DNS и файл `hosts`
3. Отправляет запрос внешнему DNS-серверу
4. Если имя плоское — ищет через NetBIOS
5. Широковещательный запрос в подсеть
6. Проверяет файл `lmhosts`
7. Если ничего не нашлось — ошибка

**Где лежит файл `hosts`:**
- Windows: `C:\Windows\system32\drivers\etc\hosts`
- Linux: `/etc/hosts`

---

## 5. Прикладные протоколы

### 5.1. FTP (File Transfer Protocol)
- Передача файлов между компьютерами
- Есть аутентификация и анонимный доступ
- Использует два TCP-соединения: управление (порт 21) и данные
- Описан в RFC 959

### 5.2. TFTP (Trivial FTP)
- Упрощённая версия FTP
- Без безопасности и аутентификации
- Работает поверх UDP (порт 69)
- Данные блоками по 512 байт
- Используется для загрузки ОС на бездисковые станции и конфигов роутеров

### 5.3. Электронная почта (модель X.400)
- **MUA** — почтовый клиент (Outlook, Thunderbird)
- **MTA** — агент передачи сообщений (пересылает письма)
- **MDA** — агент доставки (кладёт письмо в ящик)
- **MS** — хранилище сообщений

### 5.4. HTTP (HyperText Transfer Protocol)
- Базовый протокол для передачи веб-страниц
- Работает по схеме «запрос-ответ»
- Обычно поверх TCP
- Основа всего современного веба

---

## Литература (что использовал)

1. Куроуз Д., Росс К. — Компьютерные сети. Топ-Даун подход
2. Таненбаум Э., Уэзеролл Д. — Компьютерные сети
3. RFC 959, RFC 3232, стандарты IANA
